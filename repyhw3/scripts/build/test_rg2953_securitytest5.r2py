# Open a file
myfile=openfile("look.txt",True)

# Attempt to write "MZ" to the file
try:
	myfile.writeat("Z",1)

except:
	pass # action failed 

finally:
	# Close the file after our attempt.
	myfile.close()